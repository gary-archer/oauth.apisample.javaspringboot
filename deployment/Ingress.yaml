#
# The load balancer for our Java API, which exposes it over port 443
# The below host name is configured on the host PC in the /etc/hosts file
#

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: javaapi-ingress
spec:
  tls:
    - hosts:
        - javaapi.mycompany.com
      secretName: mycompany-com-tls
  rules:
    - host: javaapi.mycompany.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: javaapi-svc
                port:
                  number: 8000