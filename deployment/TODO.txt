1. Understand how to expose the API over port 80
   Right now NodePort uses a port above 30000

2. Understand Kubernetes, Minikube and DNS via host files
   Get a better API URL over HTTP, similar to api.mycompany.com

3. Get SSL working so that the call into Kubernetes from the local PC is over port 444
   Understand how to configure SSL trust

4. Use a redis client to cache tokens across the cluster

5. Get stdout logging pushing to Elastic Search
   This means pods need to be able to call localhost:9200 on the host