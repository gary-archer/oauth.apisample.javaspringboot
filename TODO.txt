1. Check issuer and audience

2. Performance breakdown
   Get rid of child log entry for authorizer - but think about serverless case
   Include JWKS lookup time in perf breakdown of API call
   Should see a small value on each call

2. Java and underscores:
- checkstyle
- serialization
- Lombok

3. CONSOLIDATION
- NodeJS to use a ClaimsPayload and be updated equivalently
- Use an injected proxy object
- PerformanceBreakdown to include JWK download time