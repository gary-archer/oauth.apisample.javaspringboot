1. Test standard authorizer in oauth.end-to-end

2. Once working, get JWKS key caching working for the standard strategy
   Use JWKSetCache if needed?

3. Handle array claims properly:
- SerializeToCache must use array for scopes - even if requires verbose code
- Regions must be an array

4. Java and underscores:
- checkstyle
- serialization
- Lombok

4. CONSOLIDATION
- NodeJS to use a ClaimsPayload and be updated equivalently
- Use an injected proxy object
